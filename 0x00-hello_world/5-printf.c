#include <stdio.h>
/**
 * main - a prgram that print a line of code using the printf fuction
 * Return: 0 (Success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
